using UnityEngine;
using System.Collections;

public class WalkAlone : MonoBehaviour {
    /*
	// Use this for initialization
	void Start () {
        Pontos.pontosGold = Pontos.pontosTotal;
    }
	
	// Update is called once per frame
	void Update () {
	
	}

    void OnGUI() {
        //Itens
        if (GUI.Button(new Rect(Screen.width / 2 - 50, Screen.height / 2, 50, 30), "Menos")) {
            Pontos.pontosGold -= 10;
        }
        if (GUI.Button(new Rect(Screen.width / 2 + 50, Screen.height / 2, 50, 30), "Mais")) {
            Pontos.pontosGold += 10;
        }

        //Pontuação
        GUI.Label(new Rect(10, 10, 200, 20), "Sua pontuaçao: " + Pontos.pontosGold);

        //Mudar de cena
        if (GUI.Button(new Rect(10, 70, 50, 30), "Jogar")) {
            Application.LoadLevel("jogo");
            PlayerPrefs.SetInt("pontosPref", Pontos.pontosGold);
        }
    }*/
}
